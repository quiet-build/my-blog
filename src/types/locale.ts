export interface Locale {
  NAV: {
    INDEX: string
    RSS: string
    SEARCH: string
    ABOUT: string
  }
  PAGINATION: {
    PREV: string
    NEXT: string
  }
  POST: {
    BACK: string
    TOP: string
  }
  PAGE: {
    ERROR_404: {
      MESSAGE: string
    }
  }
}
